import { Platform } from 'ionic-angular';
export declare class ImageViewerSrcAnimation {
    private imageAnimation;
    private element;
    constructor(platform: Platform, image: any);
    scaleFrom(lowResImgWidth: any): void;
}
